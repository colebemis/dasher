type User {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  name: String!
}

type Board {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  name: String!
  query: String!
}
