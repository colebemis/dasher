overwrite: true
schema: http://localhost:4000
documents: '**/*.graphql'
generates:
  ./__generated__/graphql.tsx:
    config:
      noNamespaces: true
    plugins:
      - typescript-common
      - typescript-client
      - typescript-react-apollo
